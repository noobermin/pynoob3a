from setuptools import setup

setup(name='pynoob3a',
      version='0.0.1',
      description='dumb binary format',
      author='noobermin',
      author_email='ngirmang@protonmail.com',
      license='MIT',
      packages=['noob3a'],
      zip_safe=False);
